You research PUBLIC company office addresses for an FEC donor dashboard. Read-only: do not edit any project file; write only your output file (below).
Input: /home/user/fec-pipeline/handoff/2026-09-24/research_targets.csv (read with pandas dtype=str, keep_default_na=False, na_values=[]). Columns: rid, employer_name (the company as it appears in our data), employer_address/city/state/zip (what we publish now), is_primary, address_trust, stratum (A_legacy_search = old AI web answer with no saved source; D_gpt5_search / E_xai_search = AI answers; G_preserved_nocache = a row carried over from an old build with no source), method, src_url, n_donors, n_filings, occupations, donor_places, donor_states, donor_names (people who file this employer; use them ONLY to confirm which organisation they work for, e.g. a firm bio page), prior_verdict/prior_evidence (an earlier spot check, may be wrong).
For each row decide which organisation the donors actually work for (names can be ambiguous: 'BOMA' for a Los Angeles director is BOMA Greater Los Angeles, not BOMA International) and find that organisation's public office address that best fits the donors (same state/metro office when the company has one there; otherwise the headquarters). Use official websites, state business registries, SEC filings, reputable directories (WebSearch + WebFetch). Verdicts:
- correct: the published address is the right organisation's current office (small suite differences acceptable; give the exact current form in the address fields anyway).
- corrected: the published address is wrong/outdated/wrong company; give the right office with a source URL.
- home_based: the business is run from a person's home (sole proprietor / tiny firm; the published address looks residential or is the donor's own filing address). DO NOT research the home or any person's residence. Give only city/state/zip, no street.
- generic_no_office: the 'employer' is not one organisation with a meaningful office (FEDERAL GOVERNMENT, US ARMY as a whole, 'STATE OF X', 'SELF', a sector word) -> no address.
- not_found: no reliable public office can be found for the right organisation -> no address.
- ambiguous: several organisations fit and the evidence cannot decide -> no address; name the candidates in the note.
Never invent or guess an address; every correct/corrected row needs a source URL you actually opened that states the address. Never research private individuals' home addresses. Keep donor names out of the note text. Address style: street as the organisation writes it, suite in the street field ('..., Suite 400'), 5-digit ZIP, 2-letter state.
OUTPUT: write a CSV (pandas, quoting minimal, UTF-8) with exactly these columns in this order: rid,employer_name,verdict,organisation,address,city,state,zip,source_name,source_url,note — one row per input rid in your range, all of them. Write it incrementally (append after every few rows) so work survives an interruption; if the file already exists, resume after the last rid in it. Final message: counts per verdict and the path.

TOOLS (this container): WebFetch is blocked for many sites, but plain curl works everywhere. Open pages with:
  python /tmp/claude-0/-home-user-fec-pipeline/d4f8f0c4-2c16-5e36-82d2-7dceffe6c272/scratchpad/research/fetch.py URL ["regex"]
(prints the page's visible text, or only lines matching the regex with context, e.g. "suite|street|ave|blvd|\b\d{5}\b"). Use WebSearch to find candidate URLs, but the web-search budget is SHARED by all agents and small: use at most ~1 WebSearch per row on average. Save searches by going direct first: the company's own site (guess the domain, open /contact or /about), SEC EDGAR (https://efts.sec.gov/LATEST/search-index?q="NAME" or https://www.sec.gov/cgi-bin/browse-edgar?company=NAME&type=&dateb=&owner=include&count=40), bizapedia.com, state registries. A search-result snippet alone is NOT a source: open the page with fetch.py and see the address on it. If WebSearch starts returning nothing, continue with direct fetches only; if a row truly needs a search you cannot make, write verdict 'not_found' with note 'UNCHECKED: search budget' so it can be redone.
