[pytest]
pythonpath = .
testpaths = tests
markers =
    db: needs a Postgres container (testcontainers / Docker)
    live: query-correctness checks against the REAL fec_db (skipped if unreachable)
filterwarnings =
    ignore::DeprecationWarning
